<section class="relative bg-canvas overflow-hidden" id="work">
  <div class="max-w-[1200px] mx-auto px-xl lg:px-3xl pt-section pb-section">

    <div class="flex items-end justify-between mb-3xl work-reveal">
      <div>
        <span class="text-ghost text-[11px] font-medium tracking-[0.14px] uppercase block mb-sm">Selected Work</span>
        <h2 class="text-[clamp(28px,3.5vw,42px)] font-light leading-[1.08] tracking-[-0.04em] text-ink">
          Four disciplines.<br />One standard: excellence.
        </h2>
      </div>
    </div>

    <!-- Project 1 — Koora (Left) -->
    <a href="/koora" class="project-showcase group block mb-3xl work-reveal-project">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-xl items-center">
        <div class="lg:col-span-7">
          <div class="relative aspect-[16/10] bg-surface rounded-xl overflow-hidden project-image-wrap">
            <div class="absolute inset-0 flex items-center justify-center">
              <span class="font-accent text-[clamp(64px,8vw,100px)] font-bold tracking-[-0.04em] text-ink/[0.04]">01</span>
            </div>
            <div class="absolute inset-0 bg-ink/0 group-hover:bg-ink/[0.03] transition-colors duration-500"></div>
          </div>
        </div>
        <div class="lg:col-span-4 lg:col-start-9">
          <span class="text-ghost text-[11px] font-medium tracking-[0.14px] uppercase mb-sm block">CGI · Motion · Direction</span>
          <h3 class="text-[clamp(28px,3.5vw,44px)] font-light leading-[1.08] tracking-[-0.035em] text-ink mb-md group-hover:text-accent transition-colors duration-300">Koora.studio</h3>
          <p class="text-muted text-[15px] leading-[1.55] mb-lg">
            <strong class="text-ink font-medium">8 years. 150+ projects. 12 countries.</strong> CGI, motion systems, creative direction. This is where I learned that geography doesn't limit ambition — it proves it.
          </p>
          <span class="inline-flex items-center justify-center px-5 py-2.5 bg-ink text-canvas text-[12px] font-medium tracking-[-0.2px] rounded-lg transition-all duration-300 hover:scale-[0.97] hover:bg-ink/90 active:scale-[0.95]">
            View case study
          </span>
        </div>
      </div>
    </a>

    <!-- Project 2 — Renders (Right) -->
    <a href="/renders" class="project-showcase group block mb-3xl work-reveal-project">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-xl items-center">
        <div class="lg:col-span-4 lg:order-1">
          <span class="text-ghost text-[11px] font-medium tracking-[0.14px] uppercase mb-sm block">Food · Product · CGI</span>
          <h3 class="text-[clamp(28px,3.5vw,44px)] font-light leading-[1.08] tracking-[-0.035em] text-ink mb-md group-hover:text-accent transition-colors duration-300">Renders</h3>
          <p class="text-muted text-[15px] leading-[1.55] mb-lg">
            <strong class="text-ink font-medium">Photoreal CGI that sells before the product exists.</strong> Food styling, packaging hero shots, 3D assets — Maya + V-Ray, every texture hand-crafted.
          </p>
          <span class="inline-flex items-center justify-center px-5 py-2.5 bg-ink text-canvas text-[12px] font-medium tracking-[-0.2px] rounded-lg transition-all duration-300 hover:scale-[0.97] hover:bg-ink/90 active:scale-[0.95]">
            View case study
          </span>
        </div>
        <div class="lg:col-span-7 lg:col-start-6 lg:order-2">
          <div class="relative aspect-[16/10] bg-surface rounded-xl overflow-hidden project-image-wrap">
            <div class="absolute inset-0 flex items-center justify-center">
              <span class="font-accent text-[clamp(64px,8vw,100px)] font-bold tracking-[-0.04em] text-ink/[0.04]">02</span>
            </div>
            <div class="absolute inset-0 bg-ink/0 group-hover:bg-ink/[0.03] transition-colors duration-500"></div>
          </div>
        </div>
      </div>
    </a>

    <!-- Project 3 — Lapatzak (Left) -->
    <a href="/lapatzak" class="project-showcase group block mb-3xl work-reveal-project">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-xl items-center">
        <div class="lg:col-span-7">
          <div class="relative aspect-[16/10] bg-surface rounded-xl overflow-hidden project-image-wrap">
            <div class="absolute inset-0 flex items-center justify-center">
              <span class="font-accent text-[clamp(64px,8vw,100px)] font-bold tracking-[-0.04em] text-ink/[0.04]">03</span>
            </div>
            <div class="absolute inset-0 bg-ink/0 group-hover:bg-ink/[0.03] transition-colors duration-500"></div>
          </div>
        </div>
        <div class="lg:col-span-4 lg:col-start-9">
          <span class="text-ghost text-[11px] font-medium tracking-[0.14px] uppercase mb-sm block">Brand · Product · E-commerce</span>
          <h3 class="text-[clamp(28px,3.5vw,44px)] font-light leading-[1.08] tracking-[-0.035em] text-ink mb-md group-hover:text-accent transition-colors duration-300">Lapatzak</h3>
          <p class="text-muted text-[15px] leading-[1.55] mb-lg">
            <strong class="text-ink font-medium">My own brand. Built from zero.</strong> Identity, packaging, e-commerce — every decision tested in the real market. Not a portfolio piece. A business.
          </p>
          <span class="inline-flex items-center justify-center px-5 py-2.5 bg-ink text-canvas text-[12px] font-medium tracking-[-0.2px] rounded-lg transition-all duration-300 hover:scale-[0.97] hover:bg-ink/90 active:scale-[0.95]">
            View case study
          </span>
        </div>
      </div>
    </a>

    <!-- Project 4 — Lab (Right) -->
    <a href="/lab" class="project-showcase group block work-reveal-project">
      <div class="grid grid-cols-1 lg:grid-cols-12 gap-xl items-center">
        <div class="lg:col-span-4 lg:order-1">
          <span class="text-ghost text-[11px] font-medium tracking-[0.14px] uppercase mb-sm block">UI · AI · New Tech</span>
          <h3 class="text-[clamp(28px,3.5vw,44px)] font-light leading-[1.08] tracking-[-0.035em] text-ink mb-md group-hover:text-accent transition-colors duration-300">Lab</h3>
          <p class="text-muted text-[15px] leading-[1.55] mb-lg">
            <strong class="text-ink font-medium">Where design meets engineering.</strong> AI-native workflows, interactive interfaces, frontend logic. I don't just design screens — I understand how they're built.
          </p>
          <span class="inline-flex items-center justify-center px-5 py-2.5 bg-ink text-canvas text-[12px] font-medium tracking-[-0.2px] rounded-lg transition-all duration-300 hover:scale-[0.97] hover:bg-ink/90 active:scale-[0.95]">
            View case study
          </span>
        </div>
        <div class="lg:col-span-7 lg:col-start-6 lg:order-2">
          <div class="relative aspect-[16/10] bg-surface rounded-xl overflow-hidden project-image-wrap">
            <div class="absolute inset-0 flex items-center justify-center">
              <span class="font-accent text-[clamp(64px,8vw,100px)] font-bold tracking-[-0.04em] text-ink/[0.04]">04</span>
            </div>
            <div class="absolute inset-0 bg-ink/0 group-hover:bg-ink/[0.03] transition-colors duration-500"></div>
          </div>
        </div>
      </div>
    </a>

  </div>
</section>

<script>
  import { gsap } from 'gsap';
  import { ScrollTrigger } from 'gsap/ScrollTrigger';

  gsap.registerPlugin(ScrollTrigger);

  const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;

  if (!prefersReducedMotion) {
    document.querySelectorAll('#work .work-reveal').forEach((el) => {
      gsap.fromTo(el,
        { opacity: 0, y: 30 },
        {
          opacity: 1,
          y: 0,
          duration: 0.8,
          ease: 'power3.out',
          scrollTrigger: {
            trigger: el,
            start: 'top 90%',
            toggleActions: 'play none none none',
          },
        }
      );
    });

    document.querySelectorAll('.project-showcase').forEach((project) => {
      const imageWrap = project.querySelector('.project-image-wrap');
      const textEls = project.querySelectorAll('span, h3, p');

      if (imageWrap) {
        gsap.fromTo(imageWrap,
          { scale: 0.92, opacity: 0, y: 60 },
          {
            scale: 1,
            opacity: 1,
            y: 0,
            duration: 1.1,
            ease: 'power3.out',
            scrollTrigger: {
              trigger: project,
              start: 'top 85%',
              toggleActions: 'play none none none',
            },
          }
        );

        gsap.to(imageWrap, {
          y: -30,
          ease: 'none',
          scrollTrigger: {
            trigger: project,
            start: 'top bottom',
            end: 'bottom top',
            scrub: 1.5,
          },
        });
      }

      gsap.fromTo(textEls,
        { opacity: 0, y: 35 },
        {
          opacity: 1,
          y: 0,
          duration: 0.8,
          ease: 'power3.out',
          stagger: 0.08,
          scrollTrigger: {
            trigger: project,
            start: 'top 75%',
            toggleActions: 'play none none none',
          },
        }
      );
    });
  }
</script>

<style>
  .project-showcase {
    display: block;
    text-decoration: none;
    color: inherit;
  }

  .project-showcase .project-image-wrap {
    transition: box-shadow 0.6s cubic-bezier(0.16, 1, 0.3, 1);
  }

  .project-showcase:hover .project-image-wrap {
    box-shadow: 0 30px 80px rgba(0,0,0,0.08);
  }
</style>
